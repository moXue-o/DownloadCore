# 下载引擎 · 优点小本本

> **定位**：只做**下载核心**——一颗能被宿主编译期吸收、成为其一部分的零件。
> **软件外壳类能力（界面、队列、浏览器扩展、托盘、通知……）本项目一律不做。**
> **核心只干一件事**：把 URL 后面的字节，原样搬到本地文件。不解析内容、不认文件类型。
>
> 记录从 **AB Download Manager** 和 **OpenDownloader（自己之前那版）** 学到的可取之处。
> 分两批：
> - **初次添加** = 核心第一版就要有
> - **后续更新** = 属于下载核心、以后版本再加的能力
>
> 每条注明来源，方便回溯。改这本小本本时，请在"更新记录"里留一行。

---

## 一、初次添加（核心第一版必须有）

### 1. 多线程分段下载 【必备核心】
- **来源**：AB + OpenDownloader
- **说明**：把文件切成若干段，多个工人并行下载，合起来快。
- **怎么算通过**：同一个文件，多线程比单线程明显快；下完的文件和源文件逐字节一致。

### 2. 动态分段（边下边分、实时加人）【必备核心】
- **来源**：AB Download Manager（safe-zone 机制）
- **说明**：每个工人定期汇报"已经稳稳写进硬盘到这里了"。如果它前面还剩一大块没下完（超过约 1MB），就把那块一分为二，派一个新工人去接后半段，老工人只下到新的分界线为止。段数是边下边变的，不是开头定死的。
- **怎么算通过**：开始只开少量工人，下载过程中工人数会自动增加；网络带宽能被吃满；分段过程中数据不重不漏。

### 3. 下载前的"对暗号"：校验服务器返回的分段
- **来源**：AB Download Manager
- **说明**：工人要某一段数据，服务器回来的必须正好是那一段（起点、长度都对得上）。对不上就立刻停下报错，绝不闭着眼往文件里写。
- **怎么算通过**：人为让服务器返回错误区间，引擎能发现并报错，不会写出坏文件。

### 4. 认得出"文件变了"
- **来源**：AB Download Manager
- **说明**：记住文件的身份证（大小、最后修改时间、版本标记 ETag）。续传前先核对，变了就不硬接，避免新旧内容拼在一起。
- **怎么算通过**：下载一半后换掉服务器上的文件，续传时能识别并处理（报错或重下），不产生废文件。

### 5. 卡住就重连，别干等
- **来源**：两者都有（OpenDownloader 已有）
- **说明**：一段时间没有数据进来，就掐掉这条连接重来，不无限期挂着。
- **怎么算通过**：模拟服务器中途"假死"，引擎能在超时后自动重连并继续，不会永远卡住。

### 6. 错误分类：可重试 / 该停下告诉你 / 没救
- **来源**：AB Download Manager（完整的一套异常类型）
- **说明**：分清哪些错能自动重试、哪些必须停下来通知宿主、哪些是整个任务失败。分类清楚才知道下一步干嘛。
- **怎么算通过**：三类错误各有明确表现，不会一股脑全当"重试"或全当"失败"。

### 7. 每段先写自己的临时文件，全部下完再拼装
- **来源**：AB Download Manager
- **说明**：每段数据写到自己独立的临时文件里；全部下完后按顺序拼成正式文件。
- **怎么算通过**：下载中途断电/崩溃后，已下的段还在、进度清楚；重新启动能接着拼完。

### 8. 下载中的文件带标记，下完再改名
- **来源**：AB Download Manager（`.part` 后缀）
- **说明**：没下完的文件带一个能认出来的后缀，完成时才改成正式名字。用户不会把"下了一半"误当"下好了"。
- **怎么算通过**：下载中磁盘上的文件带标记；完成后标记消失、变成正式文件名。

### 9. 开局切分要均匀 + 尊重"最小块"
- **来源**：AB Download Manager（splitToRange）
- **说明**：一开始怎么把文件切成 N 段：尽量每段一样大，余数摊匀；小于最小块限制就不再切，避免几百个碎块。
- **怎么算通过**：对不同大小（很小 / 很大 / 除不尽）的文件，切出来的段都合理。

### 10. 续传记录要"要么全写、要么不写"
- **来源**：AB Download Manager（原子写入）
- **说明**：续传用的进度记录，写入时要么完整成功、要么完全不动，不能在写一半时断电导致记录损坏。
- **怎么算通过**：在写记录的瞬间强制中断，记录不会变成半截坏数据。

### 11. 提前把文件尺寸占好
- **来源**：AB Download Manager（SparseFile / 预分配）
- **说明**：先按总大小占好磁盘位置，能早点发现"没空间了"，也减少碎片。
- **怎么算通过**：磁盘空间不足时，在开始下载前/早期就报错，而不是下到 90% 才失败。

### 12. 速度统计 + 限速
- **来源**：AB + OpenDownloader
- **说明**：引擎内做速度统计；限速既能限全局，也能限单个任务。
- **怎么算通过**：设定限速后，实际下载速度稳定在设定值附近；不设则不限。

### 13. 重启后自动接着下
- **来源**：OpenDownloader（recoverTasks，方向对）
- **说明**：宿主程序关闭再打开，没下完的任务自动恢复继续（接续信息由核心负责保存/读取）。
- **怎么算通过**：下载中直接关掉程序，重开后任务自动继续且进度正确。

### 14. 先探测服务器支不支持分段
- **来源**：OpenDownloader（Range: bytes=0-0 探测，方向对）
- **说明**：开始前先问一句服务器能不能分段。不支持就老实单线程下。
- **怎么算通过**：对不支持分段的服务器，自动退化单线程，不报错、不硬切。

### 15. 文件名解析要稳
- **来源**：AB Download Manager（含 UTF-8 编码名、超长路径裁剪）
- **说明**：从服务器响应里正确解析文件名，包括中文/特殊编码的名字；名字过长时按系统限制裁剪，避免建不出文件。
- **怎么算通过**：中文名、带编码的怪名字、超长名字都能正确落盘。

---

## 二、后续更新（属于下载核心，之后再加）

> 这些仍然是"下载"这件事本身的能力，不是软件外壳。第一版可以先不做。

- [ ] **自定义请求头 / Cookie / 登录凭据**——让需要登录、或防止被防盗链拦住的站点能正常下（来源：AB IDownloadCredentials）
- [ ] **代理支持**（系统代理 / 手动 / 自动）——让下载能穿过代理（来源：AB proxy 模块）
- [ ] **校验和验证**——下完核对文件完整性与正确性（来源：AB checksum）
- [ ] **镜像 / 多来源加速**——同一个文件有多个地址时，跨地址并行下（来源：AB 思路）
- [ ] **续传记录的版本兼容**——以后记录格式升级了，还能认出旧记录（来源：AB 持久化设计）
- [ ] **重定向、Cookie 保持等 HTTP 细节的细化处理**
- [ ] **限速更细**（单个连接 / 单个任务 / 全局分别限）
- [ ] **更聪明的分段策略**（按实测网速自适应增减小块）
- [ ] **稀疏文件**（在支持的平台省磁盘占用）

---

## 三、避坑提醒（来自自己那版的教训）

> 这些是重写时要专门避免的，不是优点，但值得钉在这里。

- **别一边下一边直接往最终文件里写**。进度散落、出错难恢复的根源就在这里；改成"分段临时文件 + 最后拼装"。
- **进度状态要有单一落脚点**。状态散在多处，改一处坏一处，是之前反复改不对的主因。
- **注意临时目录/路径的拼接**。之前把任务编号当成"字符"用，会拼出非法目录名（Windows 上直接建不出来）。
- **别让调度逻辑重复两套**。之前调度器和 worker 各写了一套派活逻辑，容易互相打架。
- **动态分段要挑"剩下活最多"的段来切**。如果每次只切第一段，会把同一段越切越碎，而大段只有一个人扛，带宽用不满（实测同一文件：2.6 MB/s → 8.3 MB/s）。
- **别切出碎片段**。段太小会让连接反复重建，握手和慢启动吃掉速度；动态分裂要设最小尺寸门槛。
- **要能识别并重开"变慢但没断"的连接**。连接卡到几十 KB/s 却一直滴数据时，"空闲超时"是抓不住的，得靠"慢连接看门狗"主动重开（实测结尾 3% 拖掉三分之一时间就是这个原因）。
- **总速度常常受"单连接被服务器限速"制约**，这时唯一的办法是**多开连接**。实测同一服务器：1 连接 0.5 MB/s、8 连接 3.8 MB/s、16 连接 5.9 MB/s。并发数要够，不能只开两三条。
- **收尾阶段也要保持够多连接**。别让最后几 MB 只剩一两条被限速的连接慢慢滴；允许把尾巴切细、并让看门狗管到小剩余量。
- **引擎只管榨干带宽、给出原始计数；平滑/显示是宿主的事**。引擎只提供"累计字节 + 瞬时速度"，不替宿主做平滑——免得舍本逐末，把精力放错地方。
- **服务器常对"单个 IP 的总速度"封顶**。实测同一地址：1 连接 1.2、8 连接 3.6、16 连接 5.2、32 连接 9.0 MB/s，到 32 就顶了；再加到 40/48/64 也不再涨。所以并发开到"能摸到天花板"即可，不是越多越好。
- **别盲目照抄浏览器 User-Agent**。实测把 UA 伪装成 Chrome 反被 CDN 返回 403（Go 的 TLS 指纹和 Chrome 对不上），保持坦诚身份反而正常。

---

## 四、本项目不做（越界，防止以后又被塞回来）

> 这些是"软件外壳"，不属于下载核心。留一行备忘，避免范围又跑偏。

- 图形界面、下载队列、定时下载
- 浏览器扩展、给浏览器用的本地接口
- 托盘图标、系统通知

---

## 更新记录

- 2026-09-24 建本：从 AB Download Manager、OpenDownloader 初筛，分"初次添加 / 后续更新"两批。
- 2026-09-24 修正定位：**只做下载核心**，软件外壳能力全部剔除；"后续更新"改为只保留下载核心相关的能力。
- 2026-09-24 明确原则：核心只做"把 URL 的字节原样搬成文件"，不解析内容、不认类型。
- 2026-09-24 清理特殊情况的单独列举（不把个别例子单列成条目）。
