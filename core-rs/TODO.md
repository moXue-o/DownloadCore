# 待办

> 接口/代码层面的改进记录。做完的划到"已完成"。

## 已完成
- ✅ **结果结构体**：`dc_result` 取代一堆出参（`dc_engine_download`）。
- ✅ **错误码**：`dc_error` 分类返回，不再只有字符串。
- ✅ **默认临时目录**：改到系统临时目录，不在宿主当前目录留东西。
- ✅ **核心不再隐式使用系统代理**：代理由宿主显式决定。
- ✅ 并发句柄防护、运行时复用、布局校验（C/Rust 双向断言）、容错加锁、暂停/恢复。

## 待做（"好用"层面）
1. **进度可查询**：现在只能靠回调被动拿；加一个"随时查当前进度"的接口，方便宿主做界面。
2. **结果补元信息**：补上最终地址等，方便宿主诊断。
3. **一个句柄下多个文件的接口形态**：现在同一句柄一次只允许一个下载（已加防并发保护）；
   若宿主想"一个句柄管一批任务"，需要重新设计任务级接口。

## 功能扩展（另列）
代理（默认关、宿主显式配置）、Cookie/凭据、校验和、镜像多来源、自适应并发。
